---
date: 2026-05-03
---

> "instanceof를 써도 될까?" 에 대한 내 답

---

## 핵심 질문

> "새 타입이 추가될 때 이 코드를 수정해야 하는가?"

→ **YES** → `instanceof` 냄새, 다형성으로 대체 검토  
→ **NO** → 허용 가능한 경우

---

## 허용 / 금지 기준

| 상황 | 판단 |
|---|---|
| `equals` 구현 내부 | ✅ 허용 |
| Java 16+ 패턴 매칭 | ✅ 허용 |
| 시스템 경계(직렬화, 외부 라이브러리 타입 변환) | ✅ 제한적 허용 |
| 도메인 로직 내 타입 분기 | ❌ 다형성으로 대체 |
| 새 타입 추가 시마다 분기가 늘어나는 구조 | ❌ 설계 냄새 |

---

## 대체 전략 선택

```
instanceof를 제거하려 한다
          │
          ▼
타입마다 다른 "행위"인가?
          │
     ┌────┴────┐
    YES        NO (단순 속성 차이)
     │          │
     ▼          ▼
 인터페이스에   생성자/팩토리로
 행위 메서드   구분값 주입
 추가          (enum, 상수)
```

---

## 실전 체크리스트

- ✨ **`instanceof` 뒤에 캐스팅이 따라오는가?**

거의 확실히 다형성으로 대체 가능하다

<br/>

- ✨ **`if (piece.isCannon())` 같은 메서드가 있는가?**

이름만 다를 뿐 `instanceof`와 같다. 행위 메서드로 대체한다

<br/>

- ✨ **`instanceof` 분기가 여러 곳에 흩어져 있는가?**

새 타입 추가 시 모든 곳을 찾아야 한다 — 가장 위험한 신호

<br/>

- ✨ **EmptyPiece가 호출 시 예외를 던지는가?**

Null Object 패턴으로 빈 행위를 자연스럽게 반환한다 (→ [[왜 자식 클래스가 부모처럼 동작해야 할까?]])
