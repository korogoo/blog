---
date: 2026-05-03
---
브라우저 개발자 도구에서 Network 탭을 열면 `XHR`이라는 필터가 있었다

REST API를 호출하는 요청들이 거기 모여 있었는데, XHR이 뭔지 몰랐다

`fetch()`로 API를 호출하는 코드를 봤는데, 그게 XHR과 어떤 관계인지도 궁금했다

---

## 생겼던 질문

- XHR이 무엇인가?
- `fetch()`와 XHR은 다른 것인가, 같은 것인가?
- 브라우저 Network 탭의 XHR 필터는 무엇을 보여주는가?

---

## 왜 중요한가?

> API를 만들고 나서 ==프론트에서 실제로 요청이 어떻게 가는지 확인==해야 한다

개발자 도구 Network 탭에서 XHR 요청을 보고,  
어떤 JSON이 담겨 가는지, 응답이 어떻게 오는지 확인하는 것이 기본 디버깅 방법이다

이것을 모르면 백엔드가 맞는지 프론트가 맞는지 구분하기 어렵다

→ [[02. XHR이란?]]
