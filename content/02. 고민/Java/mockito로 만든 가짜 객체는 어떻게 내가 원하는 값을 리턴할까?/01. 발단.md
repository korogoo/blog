---
date: 2026-05-24
---

테스트를 작성하면 보통 아래처럼 작성한다

```java
when(reservationRepository.findById(anyLong()))
    .thenReturn(Optional.empty());
```

DB 연결 없이 내가 설정한 값이 반환되게 설정하는 방식이다

근데 어떻게 내가 원하는 값이 반환되는 걸까?


---

다음으로 이동 → [[02. 가짜 객체는 어떻게 만들어지나?]]

